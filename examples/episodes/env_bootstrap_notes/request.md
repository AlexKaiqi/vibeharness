# Request

Add tag search support to the notes CLI and verify the harness-tag workflow.

## Acceptance Criteria

- Searching for tag `harness` prints notes with that tag.
- Acceptance validation can run without the user manually exporting `NOTES_DB`.
- Harness bootstrap writes a reusable environment file.
