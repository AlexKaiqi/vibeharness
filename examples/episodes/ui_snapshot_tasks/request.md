# Request

Make the empty-state message disappear after the first todo item is rendered.

## Acceptance Criteria

- Empty todo data renders the empty-state message.
- One todo item renders exactly one list item.
- One todo item does not render the empty-state message.
- The validation can run without manual UI inspection.
