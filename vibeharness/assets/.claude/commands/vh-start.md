Create a VibeHarness episode for this task.

Run:

```sh
python3 scripts/vh_start.py --request "$ARGUMENTS"
```

Then inspect the created episode directory and summarize:

- checkpoint id;
- episode path;
- acceptance criteria that need to be captured;
- initial visible checks to run.
